# Sticky

Make something stick on scroll

## Usage

````js
var menu = new Sticky({
  element: document.getElementById('sticky-menu'),
  offset: 70,
  type: 'absolute',
});
````

````html
<div id="sticky-menu">
  ... content here
</div>
````
